= simple_form_for(@user, html: { class: '' }) do |f|
  = f.error_notification
  = f.input :first_name
  = f.input :last_name
  = f.input :email
  = f.input :password
  = f.submit class: 'btn btn-primary'
